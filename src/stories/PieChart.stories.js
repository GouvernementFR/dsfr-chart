import { chartData } from '../assets/data';

export default {
  component: 'pie-chart',
};

export const Doughnut = {
  args: chartData.pieChart.doughnut,
};

export const Pie = {
  args: chartData.pieChart.pie,
};
